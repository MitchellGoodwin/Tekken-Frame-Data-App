<h1>Welcome!</h1>
<% if logged_in? %>
    <h1>You are Logged In, <%= current_user.username %></h1>
    <%= button_to "Log Out", logout_path, action: "delete" %>
<% else %>
    <%= button_to "Log In", login_path, method: "get" %>
    <%= button_to "Sign Up", new_user_path, method: "get" %>
<% end %>